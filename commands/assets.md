---
description: Generate all course assets in parallel — visuals + game + tool + UGC (Stage 4)
agent: visual-director
subtask: true
model: "{{tier.high}}"
---

Invoke the `atom-creator-assets` skill with the following input: $ARGUMENTS

If no slug is provided, the skill will list available specs and ask the user to pick one.
