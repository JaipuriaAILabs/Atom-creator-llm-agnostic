---
description: Run the standalone storytelling-craft audit (Agent 6 only)
agent: course-researcher
subtask: true
model: "{{tier.high}}"
---

Invoke the `atom-creator-audit-story` skill with the following input: $ARGUMENTS

If no slug is provided, the skill will list available course JSONs and ask the user to pick one.
