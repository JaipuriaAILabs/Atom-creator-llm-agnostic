---
description: Generate an interactive single-file HTML mini-game
agent: content-auditor
subtask: true
model: "{{tier.mid}}"
---

Invoke the `atom-creator-game` skill with the following input: $ARGUMENTS

If no slug is provided, the skill will list available specs and ask the user to pick one.
