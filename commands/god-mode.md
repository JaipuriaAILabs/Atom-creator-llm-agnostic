---
description: Run the entire pipeline autonomously — plan, create, audit, assets, db-insert, final-audit
agent: course-researcher
subtask: true
model: "{{tier.high}}"
---

Invoke the `atom-creator-god-mode` skill with the following input: $ARGUMENTS

Auto-resolves all creative decisions. Pauses only at visual + game hard gates (API cost). Supports `--content-only` flag to skip visuals + game.
