---
description: Show the atom-creator command reference and pipeline map
model: "{{tier.mid}}"
---

Invoke the `atom-creator-help` skill.

Runs in the primary session. Opens the HTML help page if bundled, otherwise prints a quick reference to chat.
