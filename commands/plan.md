---
description: Plan a new Rehearsal course — research, spec, approve
agent: course-researcher
subtask: true
model: "{{tier.high}}"
---

Invoke the `atom-creator-plan` skill with the following input: $ARGUMENTS

If no input is provided, ask the user: "What role and skill should this course teach? Accepts structured input (Role / Industry / Level), a URL, a file path, or a free-text topic."
