---
description: Generate an interactive course tool (self-audit, calculator, matcher, builder, analyzer)
agent: content-auditor
subtask: true
model: "{{tier.mid}}"
---

Invoke the `atom-creator-tool` skill with the following input: $ARGUMENTS

If no slug is provided, the skill will list available specs and ask the user to pick one.
