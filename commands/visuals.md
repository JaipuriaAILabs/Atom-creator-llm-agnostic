---
description: Generate course images via the 6-phase visual pipeline
agent: visual-director
subtask: true
model: "{{tier.high}}"
---

Invoke the `atom-creator-visuals` skill with the following input: $ARGUMENTS

If no slug is provided, the skill will list available specs and ask the user to pick one.
